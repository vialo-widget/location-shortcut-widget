<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen theme — shown while the process is starting.
         After installSplashScreen() runs in MainActivity, the OS swaps in
         postSplashScreenTheme (Theme.NaviGo) to host the Compose content. -->
    <style name="Theme.NaviGo.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NaviGo</item>
    </style>

    <!-- Main app theme. Compose owns the colours at runtime via MaterialTheme,
         so this only needs to provide a sensible host window. AppCompat is the
         parent (not Theme.Material3.*) because the Material Components library
         that supplies the XML M3 themes isn't on the classpath — the Compose
         material3 artifact only ships the Composable side. -->
    <style name="Theme.NaviGo" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

</resources>
