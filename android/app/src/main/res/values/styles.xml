<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Splash screen theme — shown while the process is starting.
         After installSplashScreen() runs in MainActivity, the OS swaps in
         postSplashScreenTheme (Theme.NaviGo) to host the Compose content. -->
    <style name="Theme.NaviGo.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.NaviGo</item>
    </style>

    <!-- Main app theme. Compose owns the colors at runtime via MaterialTheme,
         so this only needs to provide a sensible host window. -->
    <style name="Theme.NaviGo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowLightNavigationBar">true</item>
    </style>

</resources>
