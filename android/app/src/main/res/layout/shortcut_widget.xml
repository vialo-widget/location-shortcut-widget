<?xml version="1.0" encoding="utf-8"?>
<!--
  NaviGo widget layout — a scrollable GridView backed by ShortcutWidgetService.

  Column count is set at runtime by ShortcutWidgetProvider#buildRemoteViews
  from the current widget width. The framework handles vertical overflow:
  if tiles don't all fit, the GridView scrolls inside the widget. The empty
  state shows when the adapter reports zero items.

  Tile content lives in shortcut_widget_tile.xml and is inflated per
  position by ShortcutCollectionFactory.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <GridView
        android:id="@+id/shortcut_grid"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:numColumns="5"
        android:gravity="center_horizontal"
        android:padding="2dp"
        android:horizontalSpacing="0dp"
        android:verticalSpacing="0dp"
        android:stretchMode="spacingWidthUniform" />

    <TextView
        android:id="@+id/empty_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="@string/widget_empty_state"
        android:textColor="#FFFFFFFF"
        android:textSize="12sp"
        android:gravity="center"
        android:padding="12dp" />

</FrameLayout>
